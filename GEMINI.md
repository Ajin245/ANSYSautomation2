## Gemini Added Memories
- githubTests
- отвечай всегда на русском языке

## Актуальные задачи (Исправление багов)
- **Исправление контактов**: Добавлено автоматическое переименование (`RenameBasedOnDefinition`) и корректная настройка типа контакта для швов (`shov`).
- **Сетка**: Реализована гибкая система поиска Named Selections (включая частичные совпадения и обработку суффиксов) с расчетом `ElementSize` как `val/meshCoef` и передачей `Quantity` через `ProjectContext` для избежания `TypeError`.
- **Remote Displacement**: Реализована поддержка вращений (RX, RY, RZ) и перемещений (UX, UY, UZ) с использованием строковых значений в конфиге (`structure_type_10558.json`), которые маппятся на int-значения в коде.
- **Нагрузки**: Реализован метод `_apply_loads` для `gu_force` с применением компонентов силы.
